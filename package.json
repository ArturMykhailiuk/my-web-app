{
  "name": "my-web-app",
  "version": "1.0.0",
  "description": "AWS Elastic Beanstalk web application",
  "main": "server/src/index.js",
  "scripts": {
    "start": "node server/src/index.js",
    "build": "cd client && npm install && npm run build && cp -r build/* ../server/public/"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "aws-sdk": "^2.1691.0",
    "uuid": "^9.0.1",
    "dotenv": "^16.3.1"
  }
}
